version: '3.8'


networks:
  backend_network:
    external: true

services:
  products:
    build: .
    container_name: products_service
    env_file:
      - .env
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    networks:
      - backend_network
    